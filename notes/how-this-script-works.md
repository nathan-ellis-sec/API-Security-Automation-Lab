# How This Script Works

This script demonstrates a basic API integration using VirusTotal's public API. It shows how to:

- Authenticate using an API key
- Encode a URL for VirusTotal's lookup format
- Send a GET request
- Parse JSON response data
- Extract meaningful threat intelligence fields
- Output structured results

This is the foundation of real-world SOC and Zero Trust automation.
